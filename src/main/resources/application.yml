spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${db}
    username: postgres
    password: ${PASSWORD}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
